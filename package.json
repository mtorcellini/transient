{
  "name": "transient",
  "version": "0.1.0",
  "private": false,
  "homepage": "https://actuallydan.github.io/transient/",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "react-scripts": "^3.4.4"
  },
  "dependencies": {
    "pizzicato": "^0.6.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
