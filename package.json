{
  "name": "transient",
  "version": "0.1.0",
  "private": true,
  "main": "https://actuallydan.github.io/transient/",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "react-scripts": "1.0.17"
  },
  "dependencies": {
    "pizzicato": "^0.6.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  }
}
